
     <%= link_to "Submit a post!", new_post_path %>

           <%= render 'scores/banner_ad' %>

        <div  class="posts"  style= "clear: both;">
          <hr>
          <% @posts.each do |post| %>
            <h2><%= link_to post.title, post_path(post) %></h2>
            <h3>Published at: <%= time_ago_in_words(post.created_at) %> ago by <%= post.user.firstname %></h3>
            
            <hr class="hr_exam">
          <% end %>
         </div>

    