<% unless upgraded? %>

	<% if display_ad %>
		<%= link_to (image_tag display_ad.image_url.to_s, class: "banner_ad"), display_ad.url %>
	<% else %>
		<%= image_tag "coke_ad.png", class: "banner_ad" %>
	<% end %>
<% end %>